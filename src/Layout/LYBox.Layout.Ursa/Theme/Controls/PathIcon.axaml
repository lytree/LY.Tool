<ResourceDictionary
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ControlTheme x:Key="{x:Type PathIcon}" TargetType="PathIcon">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Height" Value="{DynamicResource IconElementThemeHeight}" />
        <Setter Property="Width" Value="{DynamicResource IconElementThemeWidth}" />
        <Setter Property="Template">
            <ControlTemplate TargetType="PathIcon">
                <Border Background="{TemplateBinding Background}">
                    <Viewbox Width="{TemplateBinding Width}" Height="{TemplateBinding Height}">
                        <Path
                            Name="PART_Path"
                            Data="{TemplateBinding Data}"
                            Fill="{TemplateBinding Foreground}"
                            Stretch="Uniform" />
                    </Viewbox>
                </Border>
            </ControlTemplate>
        </Setter>
        <Style Selector="^.Active /template/ Path#PART_Path">
            <Setter Property="Fill" Value="{TemplateBinding BorderBrush}" />
        </Style>
    </ControlTheme>

    <ControlTheme x:Key="InnerPathIcon" TargetType="PathIcon">
        <Setter Property="Width" Value="{StaticResource FluentWidthIconMedium}" />
        <Setter Property="Height" Value="{StaticResource FluentWidthIconMedium}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <ControlTemplate TargetType="PathIcon">
                <Border Background="{TemplateBinding Background}">
                    <Viewbox
                        Width="{TemplateBinding Width}"
                        Height="{TemplateBinding Height}">
                        <Path
                            Name="PART_Path"
                            Width="24"
                            Height="24"
                            Data="{TemplateBinding Data}"
                            Fill="{TemplateBinding Foreground}" />
                    </Viewbox>
                </Border>
            </ControlTemplate>
        </Setter>
        <Style Selector="^.Active /template/ Path#PART_Path">
            <Setter Property="Fill" Value="{TemplateBinding BorderBrush}" />
        </Style>
        <Style Selector="^.ExtraSmall">
            <Setter Property="Width" Value="{StaticResource FluentWidthIconExtraSmall}" />
            <Setter Property="Height" Value="{StaticResource FluentWidthIconExtraSmall}" />
        </Style>
        <Style Selector="^.Small">
            <Setter Property="Width" Value="{StaticResource FluentWidthIconSmall}" />
            <Setter Property="Height" Value="{StaticResource FluentWidthIconSmall}" />
        </Style>
        <Style Selector="^.Large">
            <Setter Property="Width" Value="{StaticResource FluentWidthIconLarge}" />
            <Setter Property="Height" Value="{StaticResource FluentWidthIconLarge}" />
        </Style>
        <Style Selector="^.ExtraLarge">
            <Setter Property="Width" Value="{StaticResource FluentWidthIconExtraLarge}" />
            <Setter Property="Height" Value="{StaticResource FluentWidthIconExtraLarge}" />
        </Style>
    </ControlTheme>
</ResourceDictionary>